<section class="main-content__body">
 <hr/>
  <%= form_for([:reset_cache, namespace, page.resource], method: :post, html: { class: "form" }) do |f| %>
    <div class="form-actions">
      <p>Isso vai limpar o cache do  IndexedDB  para o  redis. <br>O próximo load irá sincronizar com o backend.</p>
      <%= f.submit 'Reset Frontend Cache' %>
    </div>
  <% end %>
</section>